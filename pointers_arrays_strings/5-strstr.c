#include "main.h"
#include <stddef.h>

/**
 * _strstr - finds the first needle in haystack
 * @haystack: string to search
 * @needle: subtring to find
 *
 * Return: pointer to beining of substring or null
 */
char *_strstr(char *haystack, char *needle)
{
    int i;
    int j;

    i = 0;
    j = 0;
    while (haystack[i] != '\0')
    {
        while (condition)
        {
            /* code */
        }
        
    }
    
}
